---
title: Index
desc: An example page generated by middleman barfight template
bodyclass:
---

.hero
  .hero-main
    %h4 Blending technology with travel and possibly a bit of creativity!



.project-boxes
  - data.projects.projects.each do |project|
    .project-box
      .project-box-content
        %a{ href: project.url }
          %h3= project.name
          %p= project.description

      .project-box-buttons
        - if project.built.present?
          %p.built
            - if project.built.is_a? Array
              - project.built.each do |built_item|
                %a{ href: built_item.url }= built_item.name

            - else
              = project.built

        - if project.url.present?
          %a{ href: project.url }
            %button
              %i.fa.fa-lg.fa-cog
              View Project

        - if project.github.present?
          %a{href: project.github}
            %button
              %i.fa.fa-lg.fa-github
              View Source
